\doxysection{if}
\hypertarget{if-example}{}\label{if-example}Получить состояние управляющих клавиш

Получить состояние управляющих клавиш \begin{DoxyReturn}{Возвращает}
Возвращает имя конгстрольной клавиши из списка \doxylink{}{(Get\+Contol\+Key() == Control\+Keys\+::\+Left\+Ctrl) \{ printf("{}press l\+Ctrl"{}); \}  if }
\end{DoxyReturn}
